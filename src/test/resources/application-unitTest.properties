# this file contains property values for the `unitTest`, inherited from
# `development` profile.
# use below annotation configuration in the test case.
#
# ```
# @ActiveProfiles(profiles = ["development", "unitTest"], inheritProfiles = true)
# ```

